server:
  port: 9093

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hotelmanagement
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: "*"


